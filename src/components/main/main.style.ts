import styled from 'styled-components';

export const Main = styled.main`
    flex: 1 1 auto;
    display: flex;
    flex-wrap: nowrap;
    justify-content: space-between;
    padding-bottom: 60px;
`;
