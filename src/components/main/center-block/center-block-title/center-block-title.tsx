import React from 'react';
import * as Styled from '../central-block.style';

const CenterBlockTitle = () => {
    return (
		<Styled.CenterBlockTitle>Треки</Styled.CenterBlockTitle>
	) 
};

export default CenterBlockTitle;
